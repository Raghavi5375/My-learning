#include<bits/stdc++.h>
#include<cmath>
using namespace std;
int main(){
	int r;
	cout<<"Radius = ";
	cin>>r;
	cout<<"area of the circle = "<<M_PI * (r * r)<<"\n";
	cout<<"volume of sphere = "<<M_PI * (r * r * r)<<"\n";
}
