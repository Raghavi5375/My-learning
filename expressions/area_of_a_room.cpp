#include<bits/stdc++.h>
using namespace std;
int main(){
	float length, breadth;
	cout<<"Length(in meter) : ";
	cin>>length;
	cout<<"Breadth(in meter) : ";
	cin>>breadth;
	cout<<"Area = "<<(length * breadth)<<" meters or "<<(length * breadth) * 3.281<<"feet"<<"\n";
}
